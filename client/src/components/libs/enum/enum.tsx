export enum BanType {
  BAN = "ban",
  UNBAN = "unban"
}

export enum Roles {
  ADMIN = "admin",
  GUEST = "guest"
}

export enum EFavoriteType {
  DELETE = "delete",
  ADD = "add"
}

export enum EFilmType {
  CLEAR = "clear",
  DELETE = "delete"
}